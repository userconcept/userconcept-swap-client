export type SwapperItemType = {
    direction: "out" | "in";
    balance: string;
    image: string;
    imageAlt: string;
    symbol: string;
    iconArrow: any;
    currency: string;
    active?: boolean;
}
